# Strategic replan — 2026-07-31 (session 15: mandated self-check + replan)

Trigger: all six ruling axes (inbox 8758567d) answered-negative on the v1
family (LEDGER E-019..E-027); mission §Self-check falls on session 15.

## Self-check (sessions 11–15 audited against goal 1)

- Work since the last self-check: E-022 (axis 6, market selection), E-023
  (v1-b FULL reference), E-024/E-025 (fill-model calibration, P-011
  self-resolved), E-026 (axis 4b, averaging down), E-027 (axis 5, timing).
  Every experiment pre-registered with frozen bars before execution;
  verdicts audited against tool results in-session; no unverified claims
  found in the LEDGER lines (spot-checked this session against family
  files).
- Goal-1 progress: NO positive-EV variant. Best family config remains
  v1-b at ev −1.07/mkt on FULL (run 914), stationary Apr–Jul.
- Drift verdict: **not drift** — each experiment attacked a distinct term
  of the human-ruled profit identity, and the fill-model detour (E-024/25)
  was forced by an evidence gap and resolved it. But the marginal value of
  further v1-family knob experiments is now ≈ 0: every identity term the
  ruling named is measured-negative on this universe. Continuing there
  WOULD be drift. The next increment must open a genuinely new term,
  universe, or mechanism.

## Identity coverage map (what is measured vs still free)

Per-market identity (ruling 8758567d): `EV = C·g − D·L_s`.

| Term | Attacks | Status on btc-15m pinned-800/FULL |
| --- | --- | --- |
| C (completions) | repair persistence E-008/9, both-sides E-014, cadence E-013, completion policy E-020/E-020b | slack ≈ +$30/800 mkts; trigger-space dead |
| g (margin) | gate curve E-004/E-010/E-011 | per-dollar loss ≈ −8/$100, gate-invariant |
| L_s (stranded loss) | price ceilings E-019/E-021 (every X 0.08–0.45), averaging down E-026, salvage E-020b | negative at every bound; doom rate > break-even d* at every X |
| D (doom prediction) | start state E-012, liquidity structure E-022, time-of-window E-027 | unpredictable in all 3 signal spaces |
| Fill model | E-024/E-025 (probe + trade-print calibration) | acceptable capacity bound; HF ToB gross ceiling ≈ $8.5/mkt |
| **Residue VALUE** — E[1{win} − p] for a HELD share as f(price, time) | only FILL-CONDITIONED readouts exist: hold-all −0.029/sh (E-016), stranded side wins 2.2% (E-017), tilt-at-taker-prices net negative (run 900 accident) | **NEVER measured unconditionally. The last un-attacked term reachable with data on disk.** |

The unconditional version matters because every fill-conditioned number
inherits the family's self-selection (maker fills arrive exactly when the
bought side is falling). The book's own calibration — P(side wins | its
ask price, minute-of-window) vs that price — is a property of the MARKET,
not of any policy, and it has three uses:

1. It tests the single unifying explanation of the whole negative record:
   if cheap sides (longshots) are systematically OVERPRICED
   (favorite-longshot bias), then every family we killed was buying
   overpriced longshots by construction — one phenomenon, fifteen
   experiments — and the only in-rules positive region would be the
   expensive/leading side.
2. It is the measurement behind the human's tilted-finish context
   (market-context.md): a deliberate tilt is only rational where holding
   one side has ≥ 0 EV — exactly this curve.
3. If a positive region exists it feeds BOTH a possible one-sided
   variant (scope ruling needed first, see pair-v14.md §Follow-ups) and
   in-family uses that need no ruling (completion timing when the
   completing side is underpriced; start-side selection).

## Decision (ranked)

1. **E-028 / pair-v14 (this session): Phase-0 unconditional calibration
   scan** on the pinned 800 — pre-registered in
   `memory/experiments/pair-v14.md` BEFORE tool code, per protocol.
2. **Cross-symbol replication stays blocked**: re-verified today —
   eth/sol/xrp 15m and all 5m universes still have ZERO delta-typed
   conversions [db telonex_markets ⋈ conversions GROUP BY symbol,timeframe
   | 2026-07-31]. P-012 remains with the human; first action when it
   opens: replicate v1-a/v1-b + the −0.06/share invariant on eth-15m.
3. **Class-level assessment — after E-028, not before.** No class kill is
   claimable (§Kill standards: identity argument required). But once
   E-028 lands, every term of the identity has been either attacked or
   measured on btc-15m; the honest product is an ASSESSMENT for the human
   listing the residual holes by name (other universes P-012, live fill
   quality P-009, HF economics ceiling, signal spaces not yet imagined) —
   so the human can re-aim the lab with full information.
4. Deprioritized stays deprioritized: HF ToB (economics, E-025), axis 4a
   reweighting (pair-v12.md §Axis 4a reopen conditions unchanged).

Filed session 15. Next session reads this file plus pair-v14.md §Result.

## Outcome addendum (same session)

E-028 executed: POSITIVE-SIGNAL per frozen bars (first in lab history) +
the longshot-overpricing market fact; E-028b (first-touch policy): KILL
naive exploitation, favorite-side edge UNRESOLVED at n=800 (point
+1¢/share, z ≤ 1.24). Decision item 3 (class assessment) is therefore
DEFERRED until E-029 resolves the favorite-side question:

- **E-029 (next session, pre-register first)**: replicate the SAME
  frozen regions and first-touch policy on the FULL universe (~10.7k
  markets; the ~9,900 outside the pinned 800 are true OOS for the
  region selection). Local chunked scan, ~5–6 h across sessions via
  `--checkpoint`/`--time-budget-s`; no fleet cost. SE shrinks ~3.6× ⇒
  decisive on ±1¢/share. If confirmed positive: scope proposal to the
  human (one-sided buy-and-redeem variant) + in-family uses. If zero:
  the assessment (decision item 3) closes the identity map with every
  term measured.
