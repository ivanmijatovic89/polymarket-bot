{
  "note": "Mission 01 plan. One item per session: do, verify against steps, set passes:true only with evidence (run ids, DB rows, file:line), record learnings in memory, commit+push. Owner: the agent — restructure as understanding improves.",
  "items": [
    {
      "id": "smoke-local-backtest",
      "description": "Run-verify the local leg of the research loop: execute the canonical telonex-delta btc 15m backtest with --sequential on ~5 markets using an existing strategy, with RULES pins (--latency-delay-ms 140 --latency-jitter-ms 20 --protocol pair-fable --model <model-id>). Confirm what actually lands in MySQL and upgrade the seeded capability notes from code-verified to run-verified where touched.",
      "steps": [
        "Backtest exits 0 and prints a run/batch identity",
        "backtest_runs row exists with protocol='pair-fable', model set, cmd containing both latency flags and --from-ms",
        "backtest_run_markets rows and the segments row (kind='all') exist and are consistent with the printed summary",
        "memory/capabilities notes updated with [run <id>] evidence tags"
      ],
      "passes": true,
      "evidence": "2026-07-30 session 1 (of the plan). Runs 852 (5 markets) + 853 (1 market, EXIT=0 captured), strategy BuyLowPrice.v1 with RULES pins. Step 1 half-holds: exit 0 verified, but sequential mode prints NO run/batch identity (code src/cli/backtest.ts:1052-1096) -> filed P-003; identity recovered from DB instead. Steps 2-3: db run 852 — protocol/model/params/cmd (latency flags + --from-ms + injected --batchUid) all present; 5 market rows + all/daily/weekly/monthly segments match printed BATCH STATS (pnl -5, 5 maker trades, fees 0); last_n absent because <500 markets. Step 4: backtest-cli.md, metrics-storage.md, simulator.md updated with [run 852/853] tags. Bonus: maker worst-queue + all-or-nothing fill size observed; cost==buy notional for the no-sell single-buy case; tools/sql.ts built."
    },
    {
      "id": "fleet-round-trip",
      "description": "Run-verify the distributed leg: submit a small (~20 market) canonical batch to the fleet from pushed code, watch it through the queues, confirm workers process it and the aggregate persists. Re-measure per-market speed (RULES says the ~1.5s anchor is stale). Document submission mechanics, queue observation, and measured speed in memory.",
      "steps": [
        "Batch submitted non-sequentially completes without manual intervention; run id recorded",
        "Worker heartbeats and queue counts observed programmatically (not via dashboard UI) and the method noted in memory",
        "Measured markets/minute recorded in memory/capabilities/fleet.md with the run id as evidence",
        "backtest_run_markets.machine_id shows fleet machines, not this producer"
      ],
      "passes": true,
      "evidence": "2026-07-30 session 2. Runs 854 (20 markets, 21.9s incl. fleet self-update bounce) + 855 (200 markets, 19.0s from enqueue), both canonical RULES-pinned, EXIT=0, zero manual intervention, submissionUids 78363e30/b6b2edb3 -> run ids recovered via unique submission_uid column. Step 1: OK (run ids 854/855 recorded). Step 2: built tools/fleet.ts (BullMQ getJobCounts + Redis heartbeat scan + aggregate dependency counts); observed 31 live worker processes pre-batch and live SHA flip 2b73aac->6c457e4 during self-update; method documented in fleet.md. Batches <20s outran 5s polling — active-batch view verified structurally, not against a long-running batch. Step 3: measured sustained ~870 markets/min (run 855: duration_wall_clock_ms=13796 for 200 markets, avg 1.61s/market/slot, 27 slots) recorded in fleet.md. Step 4: run 855 machine_ids = 527674ef4858x74, da1482db09f6x64, 5a69e8aa2068x36, 8955f8d87c59x26 — fleet machines confirmed; the 26 on the producer machine revealed 5 unexpected worker slots there -> P-004. Bonus: small batches (run 854) skip machines mid-self-update (2 of 4 machines got all 20 jobs)."
    },
    {
      "id": "parity-boundary-map",
      "description": "Write memory/capabilities/parity.md: the definitive live/backtest parity boundary for THIS strategy — truly shared (MarketEngine, StrategyRunner, strategy code, tick semantics), simulated (fills, fees, latency, order status), and the pair-specific implications (worst-queue maker understates fill rate = safe bias; all-or-nothing maker fill size = optimistic for large orders; no MINED status; merge trap; no cash model; hardcoded risk limits). Resolve the survey's open questions (place_batch 15-cap enforcement, FOK visible-depth semantics). Define the evidence bar a backtest result must meet before it is trusted live.",
      "steps": [
        "parity.md exists; every claim carries file:line at a stated SHA or a run id",
        "Each survey open-question is resolved in code or explicitly parked with reason",
        "The live-trust evidence bar is written down (what runs, at which latencies, over which universe, with which capital units)",
        "Anything found engine-bug-shaped is in state/PROPOSALS.md"
      ],
      "passes": true,
      "evidence": "2026-07-30 session 3. memory/capabilities/parity.md written @ e96b246: shared core (MarketEngine/StrategyRunner/OrderManager/Portfolio + identical risk walls), per-intent simulated-boundary table, clock/vantage differences, 8 binding strategy conventions, 8-point live-trust evidence bar. Every claim tagged [code file:lines @ e96b246] or [run 852]. Step 2: place_batch cap RESOLVED (live-only whole-batch rejection, LiveExecution.ts:155-167; no cap in OrderManager/BacktestExecution -> P-005); FOK RESOLVED as visible-depth all-or-nothing in backtest (BacktestExecution.ts:33-48,479-503) with exchange-side internals explicitly PARKED with reason (parity.md §7). Step 4: P-005 (batch cap parity trap), P-006 (cancel id-space mismatch: backtest clientOrderId-only vs live orderId-only), P-007 (live cancelOrder swallows errors, always reports 'canceled') filed. New parity traps found beyond the survey: FOK kill event-kind differs (order_done'killed' vs order_rejected), GTD expiry reason differs ('expired' vs 'canceled'), live cancel_all is account-wide and eventless, live fee rate is WS-driven vs hardcoded 700bps, live fill visibility gated at USER_WS_FILL_AT_STATUS default MINED. Also this session: repaired PLAN.json (session 2's edit dropped the parity item's id/brace -> file was invalid JSON)."
    },
    {
      "id": "metrics-and-capital-units",
      "description": "Design the capital-aware units (mission goal: invested per market, profit per $100 invested, EV at several capital levels) and verify how each is computed from stored data. Test the key hypothesis that for a no-sell strategy `cost` (remaining basis) equals total invested, define the intent_meta stamping convention pair-fable strategies must follow for per-order analytics, and decide whether engine persistence of buy notional is needed (P-002).",
      "steps": [
        "Units defined in memory/process/evaluator.md (draft section) with exact formulas over DB columns / intent_meta",
        "cost==invested hypothesis tested against a real run's data (evidence: run id + query)",
        "intent_meta convention documented and shown to survive the dedup-per-clientOrderId behavior",
        "P-002 updated: either withdrawn (intent_meta suffices) or sharpened with the exact columns wanted"
      ],
      "passes": false
    },
    {
      "id": "tools-launch-and-smoke",
      "description": "Build tools/run-backtest.ts (canonical launcher: single and batch, enforces RULES pins, validates flags against the silent-typo hazard, supports deliberate latency sweeps) and tools/smoke.ts (local --sequential smoke of a strategy on a few markets, the mandatory pre-fleet gate). Both print machine-parsable output.",
      "steps": [
        "smoke.ts runs an existing strategy end-to-end locally and reports the run id + headline stats",
        "run-backtest.ts refuses a missing pin (latency/provenance/floor) unless explicitly overridden",
        "A deliberate typo flag is rejected, demonstrating the validation the raw CLI lacks",
        "tools/README.md updated to match reality"
      ],
      "passes": false
    },
    {
      "id": "tools-results-and-compare",
      "description": "Build tools/results.ts (run/batch summary from MySQL incl. the capital-aware units from metrics-and-capital-units), tools/compare.ts (two+ runs on the same universe: EV deltas, win rates, per-segment stability, latency-sweep table), and tools/fleet.ts (queue depths, active batches, worker heartbeats via BullMQ/Redis patterns already mapped in memory).",
      "steps": [
        "results.ts output verified by hand against direct SQL for one run",
        "compare.ts run on two real runs (e.g. smoke vs fleet run of same strategy) produces a correct comparison",
        "fleet.ts shows live queue/worker state matching bull-board",
        "All three emit --json output consumable by later automation"
      ],
      "passes": false
    },
    {
      "id": "baseline-pair-strategy",
      "description": "Implement pair-fable-v0: the simplest honest version of the RULES strategy — alternating small-increment maker BUY accumulation on both sides gated on fee-inclusive pair price < $1 (uses ctx.metrics.position pair_avg/imbalance), no sells, no merge intents, hold to settlement. Purpose is proving the loop end-to-end, not profitability. Smoke locally, push, run a small fleet batch, read results with the tools.",
      "steps": [
        "npm run protocol:check -- pair-fable passes",
        "Strategy appears in the registry (backtest accepts --strategy pair-fable-v0)",
        "Local smoke run completes; fleet batch of ~50 markets completes with provenance columns set",
        "Results read via tools/results.ts; behavior sanity-checked (buys both sides, imbalance stays within its cap, zero sells, zero merges); learnings in memory/experiments/"
      ],
      "passes": false
    },
    {
      "id": "evaluator-design",
      "description": "Design and document the evaluation system in memory/process/evaluator.md: stage pipeline (cheap screen on a subset -> full protocol universe -> upward latency sweep per RULES -> temporal holdout/walk-forward using the existing segment machinery and walkForwardRank), champion and portfolio selection criteria, the capital-aware units as the reporting standard, overfitting guards (param-count discipline, holdout hygiene), and variant-independence measurement (e.g. daily-pnl correlation across variants). Implement whatever scoring the tools don't already cover.",
      "steps": [
        "evaluator.md complete: stages, promotion criteria, units, guards — concrete enough that a fresh session can evaluate a run without judgment calls",
        "The pipeline is executed once on baseline-pair-strategy results as a dry run (evidence: run ids + the produced evaluation)",
        "Latency-sweep protocol specified with exact flag values and comparison method",
        "Independence measure computable from stored per-market rows (verified on real data)"
      ],
      "passes": false
    },
    {
      "id": "capability-refresh-procedure",
      "description": "Build the human-triggerable capability self-upgrade: tools/refresh-capabilities.ts diffs origin/main against the SHAs recorded in memory/capabilities/*.md headers over the surveyed paths (src/cli, src/backtest, src/trading, src/strategy, src/db/schema.ts, scripts/run-worker.sh, ops/), reports which notes are stale and which files changed, and memory/process/capability-refresh.md documents the procedure (when to run, how findings fold back into notes).",
      "steps": [
        "Running the tool now reports all notes current at their recorded SHAs (or lists real drift if main moved)",
        "Simulated drift test: against an older SHA the tool correctly flags affected notes",
        "capability-refresh.md written; human trigger documented in one command"
      ],
      "passes": false
    },
    {
      "id": "mission-02-review-and-ready",
      "description": "Self-audit every plan item against its evidence, then review missions/02-research.md against everything learned and built. Write state/READY.md: delivered inventory, remaining unknowns/risks, proposed Mission 02 amendments each with reason (candidates from current knowledge: capital-cap params as a required strategy convention given no cash model; evPerMarketTotal as the headline unit given flat-market classification; the extend-latency bug's impact on long-running result accumulation), what should stay unchanged, and the proposed team workflow for later parallel agent loops. Return wait with summary 'READY for review'.",
      "steps": [
        "Every passes:true item re-checked against recorded evidence; discrepancies fixed or reopened",
        "READY.md contains: delivered, unknowns/risks, Mission-02 amendment list with reasons, team-workflow proposal",
        "state/PROPOSALS.md contains every engine finding made during the mission (nothing silently forgotten)",
        "Session returns wait: 'READY for review'"
      ],
      "passes": false
    }
  ]
}
