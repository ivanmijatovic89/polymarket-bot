# 0x76d4d4703add6e94cfdb1107f3d991d85ff2c512 — the subsidy-native breadth farmer (A61)

Session 11, unit 9. The SECOND profile-less active (with 13e0d447,
A57) and the last VARIANT-ATLAS §4 dossier candidate among living
wallets. Where 13e0d447 is the deep-pair trading specimen, this
wallet is the purest living SUBSIDY-LOOP specimen — the A28 rebate
identity walking around in production.

## Identity & timeline

- No public profile ("profile not found" while actively trading);
  API-only visibility (A55 caveat: rotations into such wallets are
  invisible).
- **Born 2026-03-25** (day-probes: zero activity ≤ Mar-24, active
  Mar-25) — four days before the fee-curve reshape rollout (A52)
  and six days before its first MAKER_REBATE payout (**Mar-31 —
  the first day of the daily maker-rebate payout era**). This
  wallet was built for the subsidy regime, not adapted to it.
- Active continuously Mar-25 → today (~114 days).

## Economics (lifetime, pulled 2026-07-17)

| line | amount |
|---|---:|
| lb trading profit (all-time) | **−$97,985** |
| MAKER_REBATE total (107 daily payouts since Mar-31) | **+$137,022** |
| TAKER_REBATE total | **$0 — zero entries ever** |
| net lifetime | ≈ +$39k (~$342/day avg) |
| current 7d: rebates $1,236/day − trading ~$632/day (lb 30d −$18.9k) | **≈ +$600/day net** |

The zero-taker-rebate line is the cleanest maker-purity proof
available (the program auto-pays any tiered taker flow) and
corroborates the atlas's on-chain role scan (maker share 1.00).

## Fingerprint

- **Breadth farmer**: ~264 mkts/day (atlas); O11 snapshot: $26.7k/2h
  spread across btc-5m ($15.9k), btc-15m ($3.8k), eth-5m ($1.3k);
  clip p50 $2.55 — the smallest on the board.
- **Continuous-merge posture** (A56 axis): 666 merges/2h in O11 —
  recycles capital constantly; opposite end of the axis from
  13e0d447 (zero merges).
- Sub-$1 prints: pairCost 0.9877 in O11 (it sat sub-$1 while the
  club was empty at 15:52 — breadth + patience, not depth).
- **btc-15m sleeve (Jun-12/13 join, 171 mkts)**: outlay p50 only
  $220–380/market, pairRate p50 0.85–0.91, pairCost p50 0.99;
  trading gross −0.17→−0.47% of outlay in EVERY session (flat
  small bleed, no session structure); excessWon 26–33% —
  ANTI-informed lean (it ends up long the losing side more often
  than chance; the leftover leg is residue, not a bet). The
  farmer's trading line is a controlled cost, not an edge attempt.
- Book-level drift (3 joined cells, 4.7k fills): noisy around zero,
  no favorable-drift signature (edge wallets show +0.9c@60s, A39) —
  fill selection is irrelevant to its P&L, exactly as a
  rebate-carried book implies. Method caveat kept: the level-class
  join calls 37–42% of its fills "taker-priced" — with role-exact
  maker 1.00 + zero taker rebates, this is stale-book
  misclassification at small clips in fast tape (same
  reconciliation as A34's; treat level-class as approximate for
  sub-$3 clips).

## Reading (design axes)

Entry: book-state breadth (every book, every window, tiny clips) ·
ladder: shallow/touch-hugging · completion: maker-only, patient ·
leg-risk: indifferent (residue leg, anti-informed) · exit:
continuous merge · subsidy posture: **pure maker-rebate farm** —
trading is a −0.3%/market cost of manufacturing fee-weighted maker
volume; income = 20% pool share (A28), currently $1.2k/day.

## Lab implications

1. This is the living existence proof of the A28 subsidy loop at
   NET +$600/day with zero trading edge — the floor a maker-pure
   candidate earns if its trading line lands at breakeven-minus.
   But it needs BREADTH (hundreds of markets/day across books) to
   clear the $1/day/market payout steps; a btc-15m-only v1 cannot
   replicate this income shape (the btc-15m pool is $6.8k/day
   total, A60).
2. Farmer economics do NOT need the favorite-lean, fill selection,
   or session gating (its bleed is flat everywhere) — the modules
   that matter for the EDGE recipes are dead weight here. The two
   profile-less wallets are near-perfect complements on every axis;
   a hybrid (deep pairs where edge exists + farm breadth elsewhere)
   is bonereaper's shape (A12), now visible as a spectrum.
3. Cold-start note: born Mar-25 with no tier history, maker-pure
   from day one — replicates A32 (maker-pure newcomers bypass the
   taker-tier moat) a month before 13e0d447 did.

Sources: data-api /activity (pull
`data/activity-76d4d470-jun12-13.jsonl`, 125,444 rows), lb-api
profit, typed rebate pulls (`data/rebates-{maker,taker}-76d4d470.json`),
edge-source joins (`data/edge-source-76d4d470.log`), O11 snapshot,
VARIANT-ATLAS row (era scans).
